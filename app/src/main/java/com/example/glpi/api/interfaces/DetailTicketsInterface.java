package com.example.glpi.api.interfaces;

public interface DetailTicketsInterface {
    //Esta interfaz se encarga de coger la posición en la que el usuario pincha para
    //abrir el ticket correspondiente a esa posicion
    void onItemClick(int position);
}
